# Pinned stable toolchain; MSRV (1.88, required by let-chains in the dependency tree) is declared in
# Cargo.toml and checked
# by a dedicated CI job with RUSTUP_TOOLCHAIN.
[toolchain]
channel = "1.96.0"
components = ["clippy", "rustfmt"]
