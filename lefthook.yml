# Install once per clone: `mise exec -- lefthook install`
pre-commit:
  parallel: true
  jobs:
    - name: rustfmt
      glob: "*.rs"
      run: cargo fmt --all --check
    - name: clippy
      glob: "*.rs"
      run: CARGO_BUILD_WARNINGS=deny cargo clippy --workspace --all-targets
