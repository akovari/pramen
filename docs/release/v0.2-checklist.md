# v0.2 announce-readiness checklist

Use this before tagging `0.2.0` and pushing a GitHub Release. Every item
should be checked locally; CI covers the offline gates.

## Version and packaging

- [x] Workspace `[workspace.package] version` is `0.2.0`
- [ ] `pramen --version` reports `0.2.0` on a release build
- [ ] `cargo dist plan` still covers the four tier-1 targets + installers
- [ ] Tag `v0.2.0` triggers the release workflow

## Phase 2 Group X1 (merged)

- [x] X1.4 — OCI WASM pull by digest, allow-list, signature hook
- [x] X1.5 — Azure Blob + GCS sources + offline residency validation
- [x] X1.7 — `ai.generate` with bounded UTF-8 output
- [x] X1.8 — Postgres-backed ledger and checkpoint stores
- [x] X1.1–X1.3 + X1.6 already on main from earlier merges

## Docs

- [x] README current status + known limitations reflect v0.2
- [x] GitHub Pages (`site/`) roadmap, schema reference, cookbooks updated
- [x] JSON Schema artifact regenerated

## Still open (not blocking 0.2)

- Cloud acceptance: S1.1 live Bedrock, S2.1, S2.2 frontier, P2.1 1M run
- [x] X2.1 third-party WASM conformance proof (`examples/external-wasm-guest/`)
- [x] X2.2 reproducible AWS deployment artifacts + runbook (`deploy/`,
  `docs/deploy/aws-runbook.md`) — live apply still credentials-gated
- Cosign/sigstore on the OCI signature hook; authenticated OCI registries
